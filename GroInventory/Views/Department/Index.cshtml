@model IEnumerable<Department>

<h2>Departments</h2>


<table class="container-fluid">
    @*<tr>
        <td>
            @using (Html.BeginForm("Search", "Product", FormMethod.Get))
            {
                <p>
                    Search by Name or UPC:<br>
                    @Html.TextBox("searchString")
                    <input type="submit" value="Search" />
                </p>
            }
        </td>
    </tr>*@
    <tr>
        <td>
            <button type="button" class="btn btn-primary">
                <a class="text-light" href="/Department/InsertDepartment/">Create a New Department</a>
            </button>
        </td>
    </tr>
</table>


<br>

<table class="table table-bordered table-hover">
    <thead class="thead-dark">
        <tr class="text-center">
            <th>Department ID</th>
            <th>Department Name</th>
        </tr>
    </thead>

    @foreach (var dept in Model)
    {
        <tr class="text-center">
            <td><a href=/Department/ViewDepartment/@dept.DeptID>@dept.DeptID</a></td>
            <td><a href=/Department/ViewDepartment/@dept.DeptID>@dept.DeptName</a></td>
        </tr>
    }
</table>